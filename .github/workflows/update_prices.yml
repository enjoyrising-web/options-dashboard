bash

cat /home/claude/github_actions/.github/workflows/update_prices.yml
Output

name: 每日收盘价更新

on:
  schedule:
    # 港股收市后：每天 09:30 UTC = 港股 17:30 HKT
    - cron: '30 9 * * 1-5'
    # 美股收市后：每天 21:30 UTC = 美东 16:30 ET（冬令时）
    - cron: '30 21 * * 1-5'
  workflow_dispatch:  # 允许手动触发

jobs:
  update-prices:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Update closing prices
        run: python .github/scripts/update_prices.py

      - name: Commit and push if changed
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add data/products.json
          git diff --staged --quiet || git commit -m "自动更新收盘价 $(date '+%Y-%m-%d %H:%M')"
          git push
